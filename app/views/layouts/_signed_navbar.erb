<nav class="navbar navbar-expand-lg bg-body-tertiary bg-dark border-bottom border-body" data-bs-theme="dark">
    <div class="container-fluid">
    <div>
      <% if current_user.bio %>
        <%= link_to image_tag(current_user.bio.avatar, size:"30x30", style: "border-radius: 50%"),
        current_user.bio %>
      <% end%>
      <%= link_to "Mi muro" , root_path, class: "navbar-brand ms-4" %>
    </div>
    <%= link_to 'Close session', destroy_user_session_path,
      class: 'btn btn-danger ' %>
    </div>
  </div>
</nav>
